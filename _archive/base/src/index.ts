export * from './types';

// export const runTask = <Key extends keyof Configs, Config extends Configs[Key]>(name: Key, config: Config) => {
//   const func = require(`@gulppress/${name}`);
//   return func.default(config);
// };
