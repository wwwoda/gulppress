// import { parallel, TaskFunction, watch } from 'gulp';

// interface WatcherConfig {
//   src: string;
//   callback: TaskFunction;
// }

// export default function (watchers: WatcherConfig): TaskFunction {
//   function initWatchers(): void {
//     for (const watcher of watchers) {
//       watch(watcher.src, watcher.callback);
//     }
//   }

//   return parallel(initWatchers);
// }
