/**
 * GulpPress Env File.
 * This file should be ignored by git as it can differ from developer to developer
 *
 * @package GulpPress
 */

import { gulppress } from '@gulppress/scripts';

const browserSyncConfig: gulppress.BrowserSyncConfig = {
	ghostMode: false,
	injectChanges: true,
	open: true,
	proxy: '{{projectURL}}',
	tunnel: false,
};

export default browserSyncConfig;
